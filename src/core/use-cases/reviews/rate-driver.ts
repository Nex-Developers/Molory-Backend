export default () => {
    return async ({
        travelId,
        rating,
        comment
    }) => {
        // find
        // add or update
        // recaculate passenger rating
        // update passenger rating
    }
}